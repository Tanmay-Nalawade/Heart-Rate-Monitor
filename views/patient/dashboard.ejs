<% layout('layouts/boilerplate')%>
<h1>Hi there <%= currentUser.name %> !!</h1>

<% if(currentUser && currentUser.constructor.modelName === 'Patient') {%>

<a href="/patient/device/new" style="color: black; font-weight: bold">
  Add a device
</a>

<% } else if(currentUser && currentUser.constructor.modelName === 'Physician')
{%>

<h2>All Patients</h2>

<% } %>
<ul>
  <% for (let info of data){%>
  <li><a href="/patient/device/<%= info._id %>"><%= info.name %></a></li>
  <% }%>
</ul>
